<h1>All the gossips</h1>

<div>

<% @gossips.each do |g|  %>
<ul>
  <li> <%= g.title  %></li>
</ul>

<% end %>
</div>

<h2>Create a new gossips</h2>

<div class="form">
<%= form_for Gossip.new do |f| %>
  <div class="form-control">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="form-control">
    <%= f.label :body %>
    <%= f.text_field :body %>
  </div>
  <%= f.submit %>
  <% end %>
</div>
